#To Reproduce Same result so use set.seed
set.seed(3)
y = rnorm(100)# to generate randome number upto 30
# Find Mean
mean(y)
# Find Variance
var(y)
# Find standard Daviation
sqrt(var(y))
# Or simply use sd function to find standard deviation